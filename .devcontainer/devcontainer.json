// For format details, see https://aka.ms/devcontainer.json. For config options, see the

// README at: https://github.com/devcontainers/templates/tree/main/src/docker-existing-dockerfile

{

"name": "Existing Dockerfile",

"build": {

// Sets the run context to one level up instead of the .devcontainer folder.

"context": "..",

// Update the 'dockerFile' property if you aren't using the standard 'Dockerfile' filename.

"dockerfile": "../Dockerfile"

},


"runArgs": [


// Isso é necessário para que o driver da GPU tenha acesso

// de baixo nível ao hardware.

"--privileged",

// --- Monta AMBOS os sockets ---

// Socket do X11 (para XWayland)

"-v", "/tmp/.X11-unix:/tmp/.X11-unix:rw",

// Socket do Wayland (para apps nativas)

"-v", "${env:XDG_RUNTIME_DIR}/${env:WAYLAND_DISPLAY}:/tmp/wayland-0:rw",


// Mapeia o "convite" (cookie de autorização) do seu usuário no host

// para o usuário root dentro do container.

"-v", "${env:HOME}/.Xauthority:/root/.Xauthority:rw",


// --- Outras flags de hardware ---

"--security-opt", "label=disable",

"--ipc=host",

"-v", "/dev/dri:/dev/dri:rw"

],


"containerEnv": {


// --- Define AMBAS as variáveis de display ---

// Para X11/XWayland

"DISPLAY": "${localEnv:DISPLAY}",

// Para Wayland Nativo

"WAYLAND_DISPLAY": "wayland-0",

"XDG_RUNTIME_DIR": "${localEnv:XDG_RUNTIME_DIR}",

// --- NOVO: Diz às aplicações onde encontrar o arquivo de autorização ---

"XAUTHORITY": "/root/.Xauthority",

"MESA_GL_VERSION_OVERRIDE": "4.6"

},


"customizations": {

"vscode": {

"extensions": [
	"ms-vscode.cpptools",
	"ms-python.python",
	"redhat.vscode-xml"
]

}

}

// Features to add to the dev container. More info: https://containers.dev/features.

// "features": {},


// Use 'forwardPorts' to make a list of ports inside the container available locally.

// "forwardPorts": [],


// Uncomment the next line to run commands after the container is created.

// "postCreateCommand": "cat /etc/os-release",


// Configure tool-specific properties.

// "customizations": {},


// Uncomment to connect as an existing user other than the container default. More info: https://aka.ms/dev-containers-non-root.

// "remoteUser": "devcontainer"

}